<?xml version="1.0" encoding="utf-8"?>
<!-- 4x2 cells (n cells = 70n - 30 dp). updatePeriodMillis is Android's
     30-minute floor: the day rollover lags at most that long, and every app
     launch re-renders immediately via DailyVerseWidgetModule.refresh(). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_daily_verse_description"
    android:initialLayout="@layout/widget_daily_verse"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:previewLayout="@layout/widget_daily_verse"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
